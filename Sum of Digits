#include <stdio.h>

int main() {
    int N, sum = 0;
    scanf("%d", &N);
    if (N < 0) {
        N = -N;
    }
    while (N > 0) {
        sum += N % 10;  
        N /= 10;        
    }
    printf("%d\n", sum);
    
    return 0;
}
